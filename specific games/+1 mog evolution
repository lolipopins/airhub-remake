-- This code was generated by Cobalt
-- https://gitlab.com/upio/cobalt

--[[
    AirHub — Game / Player
    UI: rbxassetid://7657867786
    Game:
      - Auto Mog (interval 50ms) — Packet.RemoteEvent:FireServer(buffer{5})
      - Auto Clicker (interval 50ms) — PowerRemotes.ClickPower:FireServer()
      - Unload
    Player:
      - Noclip
      - Speed via CFrame
--]]

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService        = game:GetService("RunService")
local Players           = game:GetService("Players")
local UserInputService  = game:GetService("UserInputService")

local LocalPlayer = Players.LocalPlayer

-- ==== Ремоуты ====
local packet = ReplicatedStorage:WaitForChild("Packet", 10)
local RemoteEvent = packet and packet:WaitForChild("RemoteEvent", 10)
if not RemoteEvent then warn("[AirHub] Packet.RemoteEvent не найден") end

local powerRemotes = ReplicatedStorage:WaitForChild("PowerRemotes", 10)
local ClickPower = powerRemotes and powerRemotes:WaitForChild("ClickPower", 10)
if not ClickPower then warn("[AirHub] PowerRemotes.ClickPower не найден") end

-- ==== Константа ====
local INTERVAL = 0.05  -- 50 мс

-- ==== Конфиг ====
local Config = {
    Game = {
        AutoMogEnabled     = false,
        AutoClickerEnabled = false,
    },
    Player = {
        NoclipEnabled = false,
        SpeedEnabled  = false,
        Speed         = 60,
    },
    Stats = {
        AutoMogFires     = 0,
        AutoClickerFires = 0,
    },
}

-- ==== Буфер для Auto Mog: {5} ====
local packetBuffer = buffer.create(1)
buffer.writeu8(packetBuffer, 0, 5)

-- ==== Функция: Auto Mog ====
task.spawn(function()
    while true do
        if Config.Game.AutoMogEnabled and RemoteEvent then
            pcall(function()
                RemoteEvent:FireServer(packetBuffer)
            end)
            Config.Stats.AutoMogFires = Config.Stats.AutoMogFires + 1
            task.wait(INTERVAL)
        else
            task.wait(0.1)
        end
    end
end)

-- ==== Функция: Auto Clicker ====
task.spawn(function()
    while true do
        if Config.Game.AutoClickerEnabled and ClickPower then
            pcall(function()
                ClickPower:FireServer()
            end)
            Config.Stats.AutoClickerFires = Config.Stats.AutoClickerFires + 1
            task.wait(INTERVAL)
        else
            task.wait(0.1)
        end
    end
end)

-- ==== Noclip ====
local function ApplyNoclip(state)
    local char = LocalPlayer.Character
    if not char then return end
    for _, p in ipairs(char:GetDescendants()) do
        if p:IsA("BasePart") then p.CanCollide = not state end
    end
end

LocalPlayer.CharacterAdded:Connect(function()
    task.wait(0.3)
    if Config.Player.NoclipEnabled then ApplyNoclip(true) end
end)

-- ==== Speed via CFrame ====
local function GetMoveDirection()
    local char = LocalPlayer.Character
    if not char then return nil end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return nil end

    local cam = workspace.CurrentCamera
    local look  = cam.CFrame.LookVector
    local right = cam.CFrame.RightVector
    local fwd = Vector3.new(look.X, 0, look.Z)
    local rgt = Vector3.new(right.X, 0, right.Z)
    if fwd.Magnitude > 0.001 then fwd = fwd.Unit end
    if rgt.Magnitude > 0.001 then rgt = rgt.Unit end

    local dir = Vector3.new(0, 0, 0)
    if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir = dir + fwd end
    if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir = dir - fwd end
    if UserInputService:IsKeyDown(Enum.KeyCode.D) then dir = dir + rgt end
    if UserInputService:IsKeyDown(Enum.KeyCode.A) then dir = dir - rgt end
    if dir.Magnitude > 0 then dir = dir.Unit end
    return dir, hrp
end

RunService.Heartbeat:Connect(function(dt)
    if Config.Player.NoclipEnabled then
        ApplyNoclip(true)
    end
    if Config.Player.SpeedEnabled then
        local dir, hrp = GetMoveDirection()
        if dir and dir.Magnitude > 0 then
            hrp.CFrame = hrp.CFrame + dir * Config.Player.Speed * dt
        end
    end
end)

-- ==== Unload helper ====
local LibraryRef = nil

local function Unload()
    Config.Game.AutoMogEnabled     = false
    Config.Game.AutoClickerEnabled = false
    Config.Player.NoclipEnabled    = false
    Config.Player.SpeedEnabled     = false
    ApplyNoclip(false)

    if LibraryRef then
        if type(LibraryRef.Unload) == "function" then pcall(function() LibraryRef:Unload() end)
        elseif type(LibraryRef.Destroy) == "function" then pcall(function() LibraryRef:Destroy() end) end
    end
end

-- ==== UI ====
task.delay(math.random(1, 3), function()
    local Library
    local ok, err = pcall(function()
        Library = loadstring(game:GetObjects("rbxassetid://7657867786")[1].Source)()
    end)
    if not ok or not Library then
        warn("[AirHub] UI не загрузился: " .. tostring(err))
        return
    end
    LibraryRef = Library

    local MainFrame = Library:CreateWindow({
        Name = "AirHub",          -- <<< вот тут название меню
        Themeable = {
            Image = "96742921028995",
            Info  = "Game / Player",
            Credit = false
        }
    })

    local GameTab   = MainFrame:CreateTab({ Name = "Game" })
    local PlayerTab = MainFrame:CreateTab({ Name = "Player" })

    -- ===== Game =====
    local gSec = GameTab:CreateSection({ Name = "Spammers (50ms)" })

    gSec:AddToggle({
        Name = "Auto Mog",
        Value = Config.Game.AutoMogEnabled,
        Callback = function(v) Config.Game.AutoMogEnabled = v end
    })

    gSec:AddToggle({
        Name = "Auto Clicker",
        Value = Config.Game.AutoClickerEnabled,
        Callback = function(v) Config.Game.AutoClickerEnabled = v end
    })

    -- Статистика
    local gStat = GameTab:CreateSection({ Name = "Stats", Side = "Right" })
    local statMog     = gStat:AddTextbox({ Name = "Auto Mog fired",      Value = "0", Callback = function() end })
    local statClicker = gStat:AddTextbox({ Name = "Auto Clicker fired",  Value = "0", Callback = function() end })

    -- Unload
    local gAct = GameTab:CreateSection({ Name = "Actions", Side = "Right" })
    gAct:AddButton({
        Name = "Unload",
        Callback = Unload
    })

    -- ===== Player =====
    local pMain = PlayerTab:CreateSection({ Name = "Movement" })

    pMain:AddToggle({
        Name = "Noclip",
        Value = Config.Player.NoclipEnabled,
        Callback = function(v)
            Config.Player.NoclipEnabled = v
            ApplyNoclip(v)
        end
    })

    pMain:AddToggle({
        Name = "Speed (CFrame)",
        Value = Config.Player.SpeedEnabled,
        Callback = function(v) Config.Player.SpeedEnabled = v end
    })

    pMain:AddSlider({
        Name = "Speed",
        Value = Config.Player.Speed,
        Min = 10, Max = 300,
        Callback = function(v) Config.Player.Speed = v end
    })

    -- ===== Хелпер для обновления текстбоксов =====
    local function SetTB(tb, text)
        if not tb then return end
        if type(tb.Set) == "function"      then pcall(function() tb:Set(text) end) end
        if type(tb.SetValue) == "function" then pcall(function() tb:SetValue(text) end) end
        if type(tb.SetText) == "function"  then pcall(function() tb:SetText(text) end) end
        pcall(function() tb.Value = text end)
        pcall(function() tb.Text  = text end)
    end

    task.spawn(function()
        while Library do
            SetTB(statMog,     tostring(Config.Stats.AutoMogFires))
            SetTB(statClicker, tostring(Config.Stats.AutoClickerFires))
            task.wait(0.25)
        end
    end)

    Library.UnloadCallback = function()
        Config.Game.AutoMogEnabled     = false
        Config.Game.AutoClickerEnabled = false
        Config.Player.NoclipEnabled    = false
        Config.Player.SpeedEnabled     = false
        ApplyNoclip(false)
    end
end)
